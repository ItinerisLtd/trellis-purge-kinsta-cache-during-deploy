---
- debug:
    msg:
      - 'Purging Kinsta cache by HTTP GET Kinsta API'
      - "{{ site_env.wp_home }}/kinsta-clear-cache-all/"

- name: Purge Kinsta cache
  uri:
    url: "{{ site_env.wp_home }}/kinsta-clear-cache-all/"
