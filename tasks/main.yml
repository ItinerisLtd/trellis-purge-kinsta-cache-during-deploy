---
- debug:
    msg: "Going to HTTP GET {{ site_env.wp_home }}/kinsta-clear-cache-all/ to purge Kinsta cache. Confirm this URL with Kinsta support team."

- name: Purge Kinsta cache
  uri:
    url: "{{ site_env.wp_home }}/kinsta-clear-cache-all/"
